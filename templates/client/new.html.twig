{% extends 'base.html.twig' %}

{% block title %}{{ form.vars.value.id ? 'Modifier' : 'Ajouter' }} un client{% endblock %}

{% block body %}
    <h1>{{ form.vars.value.id ? 'Modifier' : 'Ajouter' }} un client</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ form.vars.value.id ? 'Mettre à jour' : 'Ajouter' }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_client_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
